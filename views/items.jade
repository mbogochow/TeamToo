layout

block content
  #wrapper
    #display
      include userbar
        
      -for (var id in items)
        - var item = items[id]
        div
          a(href='/item/#{id}') #{item.name} - $#{item.price}
      
    include footer
